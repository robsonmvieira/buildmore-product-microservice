{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": "./src", 
    "outDir": "./dist",
    "paths": {
      "#core/*": ["./core/*"],
      "#modules/*": ["./modules/*"],
    
    }
  },
  "include": ["core", "modules"],
  "exclude": ["node_modules", "dist", "build", "coverage", "typings", "*.d.ts"],
}
